# Frontend — Stock Recommender

React 19 + Vite 8 single-page application. See the [project README](../README.md) for full documentation and setup instructions.

## Quick Start

```bash
npm install
npm run dev    # http://localhost:5173
```

Requires the backend API running on `http://localhost:8000`. See `../backend/` for backend setup.
